/**
 * @amlplugins/google-firebase-admin
 *
 * Thin re-export of firebase-admin (CommonJS `export =` SDK).
 *
 * Firebase Admin SDK (server).
 */

import _sdk from "firebase-admin";
export { _sdk as sdk };
export default _sdk;
